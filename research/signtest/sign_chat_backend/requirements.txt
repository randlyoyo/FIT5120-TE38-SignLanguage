# API server (enough for mock mode)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pyyaml>=6.0
numpy
requests
opencv-python-headless

# Real models: torch (CUDA build) must already be installed.
# sign -> text (Uni-Sign, unisign/)
rtmlib==0.0.16
onnxruntime-gpu==1.22.0    # CUDA 12 + cuDNN 9 build, the CUDA torch's cu12x wheels ship. 1.30 needs CUDA 13
                           # (libcublasLt.so.13) and silently falls back to the CPU. CPU-only machines: onnxruntime
# text -> sign (SignSparK, auslan_smplx/) - the versions colab_auslan_generate.ipynb pins
torchdiffeq==0.2.5
accelerate==1.10.1
transformers==4.56.1
huggingface_hub<1.0
sentence-transformers==5.1.2
multilingual-clip==1.0.10
hydra-core==1.3.2
omegaconf==2.3.0
einops==0.8.1
lmdb==2.2.0
blobfile==3.0.0
smplx==0.1.28
scikit-learn
scipy

# dialogue backends (optional)
anthropic                  # backend: anthropic
