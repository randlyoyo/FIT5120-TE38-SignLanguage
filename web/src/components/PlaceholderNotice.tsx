export function PlaceholderNotice() {
  return (
    <div className="placeholder-notice" role="note">
      <strong>Demo data:</strong> the signs in this prototype are synthetic placeholder gestures for
      demonstrating the learn / quiz / practice pipeline — they are <em>not</em> verified Auslan
      vocabulary. Real content must be checked by an Auslan-fluent signer before use.
    </div>
  );
}
